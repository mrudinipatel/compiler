void foo (void) {}

int fun(int fff ) {
    int x; int y;
    x = x * 2 + 1;
    y = x + foo();
}

void main(void) {
    int a[2]; int x;
    a[x] = 1;
    a[foo()] = 2;

    x = foo();
    
    if( x ) output(foo());
    if(foo()) output(x, foo());
}

